wm_hyprland:

  Archlinux:
    packages:
      - rofi
      - waybar
      - hypridle
      - hyprland
      - hyprlock
      - xdg-desktop-portal-hyprland
      - polkit-kde-agent
      - qt5-wayland 
      - qt6-wayland 

  Ubuntu:
    services:
      - bluetooth

    packages:
      - cliphist
      - grim
      - gvfs
      - gvfs-backends
      - inxi
      - kitty
      - nano
      - pavucontrol
      - playerctl
      - polkit-kde-agent-1
      - python3-requests
      - python3-pip
      - qt5ct
      - qt5-style-kvantum
      - qt5-style-kvantum-themes
      - qt6ct
      - slurp
      - sway-notification-center
      - waybar
      - wget
      - wl-clipboard
      - wlogout
      - xdg-user-dirs
      - xdg-utils
      - yad

      # utilities
      - brightnessctl
      - btop
      - cava
      - eog
      - gnome-system-monitor
      - mousepad
      - mpv
      - mpv-mpris
      - nvtop
      - pamixer
      - qalculate-gtk
      - bluez
      - blueman

      # gtk themes dependencies
      - unzip
      - gtk2-engines-murrine

      # hyprcursor dependencies
      - libzip-dev
      - librsvg2-dev
      - libtomlplusplus-dev

      # hypridle and xdg-desktop-portal-hyprland dependency
      - libsdbus-c++-dev
      - qt6-tools-dev

      # hyprlock dependency
      - libmagic-dev
      - libpugixml-dev

      # xdg-desktop-portal-hyprland dependency
      - xdg-desktop-portal-gtk

      # rofi
      - bison
      - flex
      - pandoc
      - doxygen
      - cppcheck
      - libmpdclient-dev
      - libnl-3-dev
      - libasound2-dev

      # swwww
      - cargo
      - liblz4-dev
      - rustup

    conflicts:
      - dunst
      - mako
      - rofi
      - bison
