docker:

  Archlinux:
    packages:
      - docker
      - docker-compose
      - python
      - python-pip

    services:
      - docker

  Ubuntu:
    packages:
      - docker
      - docker-compose-plugin
      - python
      - python-pip

    services:
      - docker
